// Fill these in from Supabase: Project Settings > API.
// The anon key is safe to publish. Never put the service role key here.
export const SUPABASE_URL = "https://YOUR-PROJECT-REF.supabase.co";
export const SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";

// Optional. Leave empty to run the detailed report as "request" only (a demand test).
// Later: a Stripe Payment Link, e.g. "https://buy.stripe.com/xxxx". The assessment id is passed
// as client_reference_id so you can match payments to assessments.
export const REPORT_PAYMENT_LINK = "";
export const REPORT_PRICE_LABEL = "£79";
export const CONTACT_EMAIL = "hello@antara-innovations.com";
